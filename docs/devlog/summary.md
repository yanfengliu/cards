# Devlog Summary

- 2026-09-10: **An independent review found the class layer's resolver sound and its four surfaces ungated — all four closed, with fourteen mutations** — the review cleared the ordering work (22 mutations red, deaths batched, mutual damage taking no new special case) and found the rest. The class-pick screen was gated by a test that built its own `renderClassPick` call and never imported `src/ui/runapp.ts` — and *no file under `test/` named `runapp` at all*, so nothing inside it could be gated by anything; the two decisions are now exported DOM-free functions with one guard for both callers, and `tools/ui-probe/pick.ts` photographs the three cards individually at 455×430 in both themes. That probe immediately found `tools/ui-probe/run.ts` had been broken for a whole unit — it waited for `#run-map svg` and the new pick screen stands in front of that, so the repo's own run instrument had been dying on a 30-second timeout with nothing saying so. `src/render/class-terms.ts` said "the numbers come from the resolver" while `VOLLEY_SWINGS` was never imported anywhere under `src/render/`, and "twice" was typed in four places plus a fifth in `src/sim/runbots.ts`; all five now derive, and the gate *measures* the swings off a real drain rather than re-reading the constant, because a test asserting `VOLLEY_SWINGS === 2` is repaired by editing the test. Scorch was absent from the pre-commit odds entirely, so a Mage facing three units read "1 of yours will swing for 1 Power" while about to deal 1 and burn 3 more — `ARCHITECTURE.md` defines fair as "the odds were visible before you committed", and a fourfold understatement is not that. And an act that ended the fight kept announcing itself: 43 stray `fizzled` in 499 Ranger wins, "the Warchief dies" followed by "has no legal target". The rule pinned for it: **the act finishes, and the resolver announces every change it makes and nothing it does not** — so an attack with no target says nothing, a burn that lands is still announced, and a spell into an empty line still fizzles because energy was spent on it. 195 tests, `npm run verify` and `verify:run` byte-identical to before. Detail: `docs/devlog/detailed/2026-09-10-review-closed.md`.
- 2026-09-10: **Unit 10's gates were claims until this session — nineteen mutations, all red, and the three classes measured** — `test/hero-attacks.test.ts` and `test/classes.test.ts` landed at `8b4177c` carrying eleven `Mutation watched going red:` comments, and the worker that wrote them died to a rate limit before running one of them; a sentence a gate carries about itself is not evidence, so nineteen mutations were applied at this revision — the eleven named plus eight more — and every one went red, recorded in `docs/learning/gate-proofs.md` with the test's own failure text. Eight of the first fifteen matched nothing on the first pass because every source file here is CRLF and the anchors were written with `\n` — the runner's "this anchor must occur exactly once" assertion turned eight silent no-ops into eight refusals, which is the only reason they were not written down as unprovable gates. Volley turns out to have two ways to be wrong that fail on opposite halves: one attack at doubled Power keeps the event count and loses the damage, two swings inside one `apply` keep the damage and lose the checkpoint, so a body killed by the first swing's retaliation swings again from off the board; only two separate `attack` effects survives both. The save-compatibility contract turns out to rest on a list's ordering: `defaultClassId` is the first class listed, so reordering `CLASSES` to put the Ranger first breaks five tests and every log written before classes existed stops replaying as the Knight it was. `npm run measure:run -- --class <id>` over 1000 seeds each: Knight 16.50% (165/1000), Ranger 0.30% (3/1000), Mage 0.00% (0/1000), and the Mage's fights run 11.57 rounds against the Knight's 8.40 — a 1-Power swing plus a 1-point burn that Armour 1 stops, against enemies carrying Armour 1 from the Shieldwall up. Per `docs/policies/local-rules.md` no cost, stat or pool weight was touched in response. One thing a later session will trip over: `--verify --class mage` exits non-zero on "the instrument can see a difference" because no run in 200 seeds was won, which is the check working, not a code failure; `npm run verify:run` runs the Knight and is green. Documentation only — no behaviour changed, `npm run gates` 189/189 and `npm run verify` byte-identical apart from `Elapsed`, `src/engine/` left untouched for the parallel review of `8b4177c`. Detail: `docs/devlog/detailed/2026-09-10-classes.md`.
- 2026-09-09: **Board width is a setting, not a target — the design and the architecture stop prescribing one** — mutual damage had narrowed the measured player line from the predicted six to nine units to three at `even` (2026-09-07) and two at every viewport (2026-09-09), and the proposal that followed, to re-cost the pool until boards widened, was refused by the owner: "The unlimited board is just a setting. Don't just try to reach unlimited board for its own sake. I never [said] AoE is a good option. It is simply an option." `docs/design/game.md` gains an **[owner]** principle — the game presents options and measures them, and a measurement that an option is weak is information for the player, not a defect for the pool — and loses "six to nine units", "deck design is the board-size dial" and every clause calling AoE the only counter to a wide board; open questions 1 and 4 are rewritten so neither presumes a re-cost; `ARCHITECTURE.md` bounds its balance loop the same way; and the rule itself lands as the first entry in `docs/policies/local-rules.md`, linked from `AGENTS.md` above the canon block — `sync-canon`'s `checkFleet` reports `cards` current with no unlinked warning against a scratch root holding this branch's files, while the real check reads the main checkout and cannot see the link until this merges — and the warning could not fire either way, because the FLEET-CANON marker line itself contains the string the check looks for; the link is verified by reading the diff, and the vacuous check is a fleet-script finding. The design's status line, which had said **unprototyped** for two days after the engine, the fight and the run existed, now says playable. No rule of play changed and no code: `npm run gates` 156/156. Still carrying the old framing, out of scope: the header comment in `src/content/cards.ts`, a comment in `test/casting.test.ts`, the unit 0, 4 and 6 plans (history), and `ARCHITECTURE.md`'s own status line, which still says there is no UI and counts four gates.
- 2026-09-07: **Units 6 and 7 reconciled: the glossary lost Ward the way it was built to, and the panel now explains the trade** — unit 7 was cut before Ward was removed, so merging it made `TRAIT_TERMS: Readonly<Record<Trait, Term>>` a type error rather than a silent lie, which is the property that table exists for. Three errors, all named by `tsc`: the `ward` entry, the `ward` icon, and two `warded: false` test fixtures. Unit 7's structure won everywhere the two touched the same file — the derived `traitPipsOf`, the derived `traitRule`, the scored panel placement — and unit 6's behaviour won everywhere else, so `git`'s own three-way merge left `src/render/board.ts` and `src/ui/app.ts` correct after only the two declaration conflicts were resolved in unit 7's favour, rather than the seven errors a wholesale `--theirs` produced. **Retaliation is now explained on every card**, because a rule the player can only meet by losing a unit to it is exactly what the panel exists for: the summary line under the number strip says attacking costs you, the hero's says it is the one exception, and the Power chip carries the whole rule. It went in that line rather than in a rule row of its own **because a row costs 54px and the panel had 0 to spare** — 266px against a 270px band — and the same sentence in the existing paragraph costs nothing. Three new gates, five mutations watched going red, one of them a character budget on that paragraph so the next sentence added to it goes red instead of silently covering the board. `npm run probe:ui hover` still reports **0 px² of opposite-row coverage in every hovered state and 2,341 px² worst own-row**, the same numbers unit 7 recorded. Hatching still off by default; the seven heraldry goldens untouched. What the merge cost: the probe can no longer reach a wide line — bodies die to the retaliation their own attacks draw, so the player's line tops out at 3 units and the 44px compression floor is now unreachable through real play.
- 2026-09-07: **A card now explains all of itself, and the panel that does it stopped hiding the board** — the owner asked what "Ward", "Wake" and "hue" meant, about mechanics in their own game, which is a card failing to say rather than a memory failing to hold: every noun here is invented and the only place any of them was written down was `docs/design/game.md`. The hover panel now names every element a card draws — Power, Health, Energy, Armour, target chance, race, field tincture, charge, card shape, and every trait in plain words — each with an icon drawn in code as nineteen path strings, because `ARCHITECTURE.md` has no image assets and an icon font would be the pipeline that rule exists to avoid. **The load-bearing property is that nothing is hardcoded**: `render/glossary.ts` keys its trait table by `engine/state.ts`'s own `Trait` union, so deleting Ward makes the file stop compiling instead of leaving a tooltip describing a rule the game no longer has, and the sentences interpolate `RELAY_POWER`/`WAKE_POWER` rather than retyping them. Two defects fell out on the way. The old preview covered **24,355 px² of the enemy row** when you hovered one of your own units — it answered "what is this card" by deleting the board you were comparing it against — and placement is now *scored* over seven candidates by the area each would cover, which put every hover at **0 px² of the opposite row**; panel height became a design constraint rather than an afterthought, since the free bands at 1440x900 are 120/180/270px and the panel had to fit 270. And heroes showed no panel at all, because `showInspect` returned early on `isHero` — the card whose Health *is* the fight explained nothing. Colour stopped being the only carrier of tribe: gules (dwarf) and vert (elf) stand next to each other on the player's own line and are **33.5 apart out of 441** once deuteranopia is simulated, so `heraldry/tinctures.ts` gained Petra Sancta hatching — vertical for gules, diagonal for vert, horizontal for azure — behind a HUD switch that is off by default because the seven reviewed goldens are bound to unhatched bytes. The number is a tripwire and the screenshots are the evidence: `.probe-ui/hover-even-7-{light,light-hatch}/13-deuteranopia-player-row.png`, and the probe feeds `feColorMatrix` the same nine coefficients `simulate` uses so the picture and the number cannot disagree — the first version used a cruder approximation and its two arms differed by more than the variable under test. `test/explain.test.ts` (20 tests, suite 137) was made to go red four ways; two of its checks were worthless on the first attempt and `docs/learning/gate-proofs.md` records why.
- 2026-09-07: **The board trades now, Ward is gone, and placement is still a decision at half the size** — four owner rulings on combat, all rules changes. **Combat is mutual**: an attack applies both blows inside one `apply` and the single checkpoint after it announces whichever died, so neither death can cancel the other's blow and both numbers come from Power read before either lands. Three sub-decisions the ruling forced, each documented with its reasoning: a **hero retaliates when struck but takes nothing back when it attacks** (units never take orders, so retaliation on a swing the player cannot withhold is unavoidable chip damage with no decision attached — measured, a hero swinging into 1-4 Power bodies for ~8 rounds a fight across ~12 fights cannot survive it); a **0-Power body retaliates for 0 and the event is still emitted**, which makes a high-Health 0-Power unit a genuine wall; and **retaliation is not an action**, so no after-acting trait fires for being hit. The consequence nobody asked for is the largest thing in the round: **a unit that dies to the retaliation its own attack drew never reaches `afterAct`**, so a fragile Relay body hands nothing forward — that is the existing "no unit acts after dying" rule meeting the new trade, it was kept rather than special-cased, and it destroyed the design's own worked example, which is rewritten around Wake and now has a real decision in it (5 damage against 3, and in one arrangement the Troll dies on its own turn to the Knight's retaliation). **Ward removed entirely** — trait, field, effect, event, card, and every mention in the design. Six gates used it incidentally to build a fixture and were **retargeted rather than deleted**; one could not be and is **recorded as lost**: "inside one unit, traits fire in source order" was gated by a unit carrying Relay and Ward, the only two shipped traits ever keyed to one event, and with Ward gone the property is unobservable — `ARCHITECTURE.md` now says so instead of implying a gate holds it, and a replacement test goes red the day a second `afterActed` trait is written. **Wake is alive**, verified rather than assumed: the woken unit must now *swing* at its raised Power, and an ablation over 200 real fights requires some outcome to differ — it used to change the outcome of not one fight in 20,000 and now carries **4.28 pp** of the placement gap on its own. **Decks reshuffle**, with no `discard` field: the reshuffle only ever runs with the cursor at the end of the deck, so the discard is exactly deck-minus-hand and there is no new state for a replay to get wrong. **Every number moved and the direct answer is: placement is still a decision, at roughly half its old size.** Over 20,000 seeds the optimal-vs-random gap is **8.38 pp** (CI 7.80..8.96) where it was 18.80, against a noise floor of -0.21 pp and a cascade-stripped control of 0.83 pp at a matched baseline — ten times the control, forty times the floor, interval nowhere near zero. The ablation's *shape* changed more than its size: it used to be **Ward-dominance at 2.5:1**, and now no trait dominates — Relay 5.99 pp and Wake 4.28 pp standalone. `npm run measure:ablate` is a tracked command for the first time; the previous round's version of that table lived in untracked scratch and could not be re-run. **The run was unwinnable at 0 of 1000 until hero Health moved 80 → 200**, and the degeneracy gate in `verify:run` is what caught it — nothing was too hard, every encounter still won 87-100% in isolation, but a won fight costs roughly twice the Health it did and the enemy's deck no longer runs dry. Run win rate 16.80% → **10.60%**, act clears 89.9/64.3/29.1 → **100.0/81.6/13.0**, routing worth 8.80 → 5.70 pp. Suite **117 → 133**, sixteen mutations watched going red. Left open and flagged rather than fixed: the cheap bodies are not costed for a world where attacking has a price, the four preset encounters are no longer centred (`easy` and `trivial` are both at the ceiling), `MIN_GAP_PP` keeps its 5.00 floor with headroom down from 5.1 sigma to 1.6 — lowering it because the thing it defends got smaller is how a gate stops being one — and `src/ui/` and `src/render/` had to be edited to remove Ward at all, with every line of that listed in the plan for the worker who owns them.
- 2026-09-07: **Spells and equipment exist, and AoE made simultaneous-death order player-visible for the first time** — the design has three card types and only units were built, which meant the one thing it names as the counter to a wide board did not exist while going wide was the strategy the deck economy rewards. Four verbs added, and the switch that turns a spell's data into them is over the *verb*, never over the card: `damageOne` (one random legal enemy target, `legalTargets` so Guard and Ward apply, the card's number less armour), `damageAll` (every living enemy unit, armour per target), `buffAll` (every living entity on the caster's line, hero included), `equip`. Five spells, three pieces of equipment — including the worked example's own **Iron Sword**, so `docs/design/game.md`'s "2 base + 3 sword = 5 Power − 2 armour = 3 damage" now runs with the card it is written with instead of a Knight given base Power 5 to stand in for it. **The load-bearing property is that an AoE is ONE effect**: every target is damaged inside a single `apply` and the checkpoint runs once after it, so both deaths land at one checkpoint in board order and the Wake triggers answering them queue in that same order. Before this, `checkStateBased`'s ordering was gated only through a fixture with a unit parked at zero Health — the *state* AoE produces, not AoE — and reversing its index loop now costs the AoE test as well; the mutation that marks the dead inside `damageAll`'s own loop, which is what an implementation that had not read the resolution contract would write, interleaves the deaths **and silently drops both Wake triggers**, because a death announced by hand never reaches `triggersFor`. Equipment is three fixed slots on the hero, replaced within a slot and cleared by a new `endFight` at every terminal result; it is a **summand** in `power()` and the new `armourOf()` rather than a write to `basePower`, so taking a piece off is exact by construction and `basePower` keeps meaning "printed", which the worked example depends on. Conservation got its first check — `ARCHITECTURE.md` has listed "energy spent never exceeds energy available" as an invariant since it was written and nothing enforced it; `selectPlays` caps a bot's spend so it can only fire for a hand-built round, which is what a UI produces. **Everything is additive at the byte level and that is a gate, not an intention**: `entityToCanonical` appends its equipment segment only when something is worn, `RoundRecord.casts` is left absent rather than empty when a round casts nothing, `CardPool.castable` is optional, and `runRound`'s new parameter defaults to `null` — so `npm run verify` is **identical line for line apart from its wall-clock line**, same 15.25 pp gap, same 2,167/4,238 differing placements, same 40 distinct hashes. Suite 55 → 86; **22 mutations applied one at a time and every one watched going red**, recorded with their failure text in `docs/learning/gate-proofs.md`. Three rules the design does not state had to be guessed and are flagged for the owner, each gated as it stands so reversing one goes red rather than passing quietly: armour applies to spell damage, AoE hits units and never heroes, and AoE ignores Guard and Ward — that last one is a decision about Ward's *scope*, taken specifically so nothing here touches the open question about Ward's *timing*. Wake, the worked example's targeting and Ward's timing are all untouched and their tests pass unchanged; Wake is still inert for exactly the reason it was, because an AoE kills the opposing side's units and that side's `startTurn` still runs before it next acts. Not merged, not reviewed, and no shipped spell is in a measured deck — nothing here is a balance claim.
- 2026-09-07: **The run exists, and deck size — not enemy Health — turned out to be the difficulty dial** — `src/run/` builds three acts, a branching map and the loop that carries a hero and a deck from the first node to the final boss, composing `src/engine/fight.ts` through its existing API with no engine change; the forge reaches a fight through the `CardPool` seam by handing it deck *instance* ids (`u_squire#7`) rather than card ids. The map is a map structurally, not statistically: **no two nodes in one row share a type**, so a row of three is always a choice between three different things, and `pathSpread` computes exactly — by DP over the rows, not by sampling — that the route decides **5.82 of 7 node types** on an average act map, 0 of 120 maps with none. Determinism holds for a whole run: same seed and agent produce one hash over 40 seeds x 3 runs, and `replayRun` reproduces it from the seed plus the choice list with **no agent in the loop**. The run generator is one `Rng` on the stream `run`; **no fight reads it** — a fight's seed and encounter are `mixSeeds(runSeed, act, nodeId, tag)`, so burning 37 draws on the run stream moves no fight seed and two differently-routed runs agree on **181/181** shared fight nodes. Headline over 1000 seeds, greedy router with search placement: run win rate **16.80%** (CI 14.61..19.24), act clear rates **89.9 / 64.3 / 29.1** conditioned on entry, and a won run is **12.1 fights of 7.8 rounds** across 24 nodes — the fight count the 30-minute target has to fit into, since minutes are not measurable headlessly. Routing is worth **8.80 pp** of run win rate at fixed placement (CI 5.96..11.64), which is the behavioural counterpart to the structural branching claim. Two content findings the design should absorb: **the enemy hero's Health is very nearly a no-op** (act-1 elite 38.7/36.7/36.3% at 16/18/20 Health; act-3 boss 29/29/29% at 30/34/40) while the **enemy deck size falls off a cliff between eight and ten cards**, so `src/content/cards.ts` calling enemy Health "this probe's difficulty dial" is true of that probe and not of the run; and **neither deck reshuffles inside a fight**, so deck size is a resource budget and "deck thinning as a skill" is inverted — a 10-card starting deck won 0 of 200 runs, as did an all-1-cost 14-card deck that cannot beat Armour 1. Thirteen mutations watched going red in `docs/learning/gate-proofs.md`; two of them were written *because* the first version of a test could not fail against the defect it named — "the map is a function of the seed" compared `hashMaps` after perturbing a generator that had already been used, and "Health carries" asserted only that Health never rose, which a run that never writes Health back also satisfies. Suite **55 → 78**; `npm run verify:run` added and chained into `npm run gates`; `npm run verify`'s fight numbers byte-identical before and after. Sigils stay out of scope with an inert seam that `hashRun` covers.
- 2026-09-07: **The game is playable, and playing it found the odds display lying** — `src/ui/` and the rest of `src/render/` land, with `npm start`, a dev server that strips types on the way out so there is no build step, and a fight a person can play end to end in a browser. I played one: seed 42, `even`, six rounds, won at 30 of 30 Health, and round two was `docs/design/game.md`'s own worked example arrived at from the board rather than the document — an Orc Shieldwall with armour 1 was eating four separate 1-Power hits for nothing, so a Berserker with a Relay Squire on its left swung for 6, dealt 5 through the armour, and killed it outright. **The screen was showing the wrong odds and it took playing to see it.** `ARCHITECTURE.md` defines fair as "the odds were visible before you committed", and the pre-commit board said "each attack is 100% onto your 1 Guard" in the single most common case there is: Ward is granted during your *own* resolution, so a Ward on your only Guard leaves `legalTargets` with nothing and every enemy attack fizzles — 0% onto everything, the opposite of what was displayed. `projectOwnPhase` now applies the Wards and Relays the committed line will grant before any chance is computed; it is exact and rolls nothing, because during your own phase nothing on your side can be hurt so every grant lands regardless of any targeting roll. Wake is deliberately not projected, since it answers a death and which of your units dies is a roll. That same lock is a **design finding for the owner, not a bug I fixed**: Ward-on-your-only-Guard is not a specialist's edge case, it is the strongest opening two cheap cards can buy, and it took me to zero damage for a whole fight while the harness stumbled into it unprompted on `hard`. `src/engine/resolver.ts` already noted Ward-after-Guard as uncovered by the design document; what playing adds is that it is the default line. The engine is untouched — `npm run verify` is identical line for line to its pre-round output apart from `Elapsed` — which cost a decision: `runRound` drops the event stream the animation needs, so `src/ui/session.ts` re-runs the round from exported pieces and carries its own `settle` and enemy turn, and that duplication is **gated rather than accepted** by comparing `hashFight` after every round across 160 fights. Render derives its own view state from the events and adds no `GameState` field, checked against the engine's own state at the end of every phase in 120 fights and at runtime. One thing the event stream does not carry: `powerGained` has `{uid, amount}` while the `gainPower` effect has `sourceUid`, so the arrow a Relay draws is reconstructed from the position of the event in the stream — **recommended engine change, one field on `powerGained` and `warded`, no behaviour and no state**. Suite **55 → 63**, all six new gates watched going red. Four of the five defects the visual pass found are invisible to every one of them, which is the point: a skip control that skipped one phase of three, a header reading the engine's end-of-round clock above a board showing mid-round, a row whose `overflow-x: auto` quietly clipped its own acting ring, and an overflow measurement sampled while a `width` transition was still gliding. Compression measured rather than eyeballed at 14 units across six viewport widths, 1440 down to 560: one distinct card top at every width, so the row never wraps, and below the 44px floor it scrolls with the hero pinned. Known gaps: 14 units is the widest line the shipped deck and 3 energy actually produce, so the 15-slot case is covered by squeezing the viewport instead; the blazon table lives in `src/render/blazons.ts` and belongs on the cards in `src/content/`, which another lane owns this round.
- 2026-09-06: **Ward-dominance survives; "Relay barely moves it" does not** — the whole measurement re-run on the repaired instrument, and every number in `docs/work/1_turn-prototype/plan.md` replaced rather than annotated. Headline over 20,000 seeds: optimal **58.30%** against random **39.51%**, a paired gap of **18.80 pp** (CI 18.06..19.53) against a noise floor of **-0.26 pp** (CI -0.98..0.47); against append-right, the honest figure for someone not thinking about placement, **11.89 pp**, up from 9.47. The sweep changed *shape*, not just size — `even` 18.32 pp now edges out `hard` 17.42 pp, where the old instrument put `hard` on top, because a search no longer ranking its own rollout noise gains most where there is board to arrange rather than where the random arm wins 6% of the time. The retracted finding was re-tested properly. The old per-trait ablation matched operating points by picking whichever of four preset encounters had the nearest baseline, leaving mismatches of several points; this one reads the enemy hero's Health as a **continuous dial**, calibrates each ablated deck on Bot B alone to find where it lands, and runs A/B/B2 at the Health values bracketing a 40% baseline, 20,000 seeds a cell. At matched baseline the intact gap is **18.65 pp**; removing **Ward** costs **10.84 pp** and removing **Relay** costs **4.25 pp**, so **Ward-dominance holds at about 2.5:1** and holds in both removal orders (12.89 vs 6.30 with the other trait already gone). But **"Relay barely moves it" is dead**: Relay carries a quarter of the gap, nearly three times the entire cascade-stripped control, and the two traits interact positively by 2.05 pp. The design consequence narrows rather than flips — placement is mostly protection, and buff routing is a real second component. The stripped set at that same 40% baseline runs **1.51 pp**, which is a better negative control than any preset cell, and the preset `easy` row at 2.56 pp (CI 1.50..3.62) retires the old "act order is worth at most about 1 pp". Wake is inert as before but the old wording was too strong: placements and outcomes are identical across 3 bots x 20,000 seeds, yet **492 of 60,000 final states differ**, and field-by-field the *only* thing that ever differs is `player.bonusPower` on a fight that ended inside the enemy phase that granted it — a +2 frozen at the buzzer, with the `combat` generator at the same position in both runs, so it never bought a roll. Measuring that needed one more instrument fix: `hashFight` serialises each entity's trait list, so comparing hashes across an ablation measures whether the ablated card was alive at the end, not whether the trait did anything — 5,644 of 20,000 hashes "differ" for Bot A against 174 real state differences. Docs corrected to match the code: `ARCHITECTURE.md` said "trigger order is board order" without saying board *index* rather than uid and without the within-unit tie-break (source order of the `if` blocks in `triggersFor`), named three banned APIs where the gate now covers four, and called the gap gate aspirational when it has been a real two-condition gate since this morning; `AGENTS.md` understated the same two gates. `src/sim/bots.test.ts` moved to `test/`, where it should have been — coverage re-checked rather than assumed, with `npm run typecheck` made to go red on a deliberate error in the moved file. The ablation probe itself is untracked scratch under `.probe/`, so that one table cannot be re-run from a clean checkout; promoting it to `tools/` is the integration owner's call and it is flagged in the plan.
- 2026-09-06: **Six of the twelve new gates were not gating what they claimed** — an independent review of the resolver-ordering round confirmed the `extraTriggers` seam is sound (byte-identical `verify` with it removed, no hot-path cost) and then found holes in the gates themselves; all closed, suite **47 → 55**, every new gate watched going red. The board-order gates were pinned to the shipped loop **by nothing but where the seam call physically sits**: every one of them is built from the seam alone, so hoisting `extra(...)` into a correctly-ordered pass of its own and then reversing the shipped `for (side) / for (entity)` nest left the whole suite green **with the shipped board-order loop inverted**. One test now puts a shipped trigger and a seam trigger on the same event — a `watchDeaths` unit at index 0 and a Wake unit at index 2 answering one death — and it is the only test that fails under the hoist, with or without the reversal. Two sibling orderings turned out to be reachable from **Wake alone, no seam at all**, and were completely ungated: which of two simultaneous deaths is announced first is decided in `checkStateBased`, not `triggersFor`, so the board-order gate never covered it — and AoE, which `docs/design/game.md` names as the only counter to a wide board, is the first designed effect that puts two units at zero at once. Likewise `drain` has **two** reaction sources and "continuations before reactions" gated one: an acting unit with Wake whose left neighbour is a corpse makes one `act` both spawn a continuation and produce a death, and with the `deaths` loop hoisted it swings at 5 instead of its printed 3. Then a latent crash made unreachable by contract rather than by luck: all five `apply` cases called `requireEntity` before their `!e.alive` guard while `checkStateBased` removes dead non-heroes from the board, so **the same sequence threw at a unit and passed silently at a hero** — one situation, two answers, decided by a filter that exists to anchor the hero at the right end of the line. It is reachable today through `resolvePhase` with a trigger keyed to `acted`, which is exactly `Echo`'s shape, and it now skips: `findEntity` plus the guard every case already carried. The throw could not have been a diagnostic anyway, since `findEntity` returns null identically for "died and was removed" and "never existed". Behaviour held still and was checked rather than asserted — `npm run verify` is identical line for line before and after, same 2,454 compared rounds, same 40 final hashes, same 15.25 pp gap. Also gated: the seam's `null` default (a no-op `TriggerRule` is behaviourally indistinguishable, so that one test reads the declaration and the four production call sites, and says so as its bound) and the iteration cap's exact boundary, both sides of it. Last, `docs/learning/gate-proofs.md` — the one file whose purpose is auditability against a named revision — said "of 44" throughout while the merge it describes is 47; every entry now names the revision its numbers were taken at, and the resolver-ordering entry carries a re-run column. Its over-claim is narrowed too: "these three properties cannot be reached from the shipped traits at all" was a statement about `triggersFor` written as though it were about resolution order, and the two ungated siblings above are the counterexamples.
- 2026-09-06: **The measurement was right and the instrument was not** — four instrument defects an independent review found in the placement probe, all repaired, and the headline moved **up**. The gate that exists to defend the design's central claim could not go red on it: `npm run verify` set a non-zero exit for card-identity and determinism failures only, so a gap that collapsed to zero still exited 0 while `ARCHITECTURE.md` described it as "a number that can go red in CI". It now fails unless the paired 95% interval excludes zero **and** the gap reaches **5.00 pp**, two conditions that bind at opposite ends of the seed count — the interval is what keeps a 20-seed run from passing on luck, the floor is what keeps a 20,000-seed run from passing on 0.74 pp. The floor comes from the measurement's own null arms rather than from the headline: the cascade-stripped control runs 0.92 pp and the noise floor -0.26 pp, so a real collapse lands at or below about 1 pp, while the paired standard error at the gate's 400 seeds is 2.7 pp — 5.00 pp is one half-width above zero and 5.1 standard errors below the measured gap, so a false red needs a 5-sigma excursion. Three red proofs, and the third is the one that matters: run unmutated at `--encounter trivial --seeds 20000`, the interval is **1.28..1.83** and excludes zero, so the statistical condition passes on a gap a fortieth of the headline and only the floor fires. A gate written with the interval alone would have called that green. Then the search itself: it scored candidates under **independent** noise, four rollouts each, so it was ranking luck as often as arrangement. Switching to **common random numbers** — every candidate in one decision rolled out against the same targeting draws — is worth **+2.42 pp paired** (CI 1.89..2.94) over 20,000 seeds on its own, taking Bot A from 55.89% to 58.30% and the headline gap from **15.87 pp to 18.80 pp** (CI 18.06..19.53). The previously reported figure was *conservative*: the design claim is stronger than the probe reported, not weaker. Both bots were also stateful closures — a `salt` counter and a generator that outlived a fight — so a policy's decisions depended on its instance's lifetime, and the measurement reproduced only because `measure.ts` happens to construct one per fight. Hoisting that construction out of the loop for speed would have changed every placement and every final hash with every gate green. Both are now pure functions of the fight they are handed, and `src/sim/bots.test.ts` fails on a warmed instance, on a hoisted instance, and on a policy that answers the same question twice differently — all three go red against the previous revision. Last, `gate:banned-apis` learned `new Date()` and bare `Date()`, the same hidden input as the three names `ARCHITECTURE.md` happens to list. Scoped to `src/` and `test/` rather than exempting `tools/heraldry-probe/shoot.ts` by name, because `gate:boundaries` already makes `tools/` unreachable from the engine **by construction**, so a probe stamping its manifest with a timestamp needs no special case now or later. Its self-test counts per rule rather than in total, since a total-only count passes with one rule dead and the other over-firing — proved by breaking the clock detector and watching it exit 2. Every proof is in `docs/learning/gate-proofs.md`. One consequence for whoever integrates: `docs/work/1_turn-prototype/plan.md` and this log's own 15.87 pp entry are now the *old* instrument's numbers, and the difficulty sweep and per-trait ablation in that plan were all measured with the salted search.
- 2026-09-06: **34 green tests, 10 defects they could not see** — an independent review found the engine correct and the suite weak: of 28 mutations reintroducing a defect the code or docs claim to prevent, ten left `npm test` at 34/34. Six of the ten were the resolver's ordering contract, and the reason they were invisible is more interesting than an oversight — they are **unobservable with the shipped pool**. Every trigger in the game is keyed to one uid, so no event can ever match two units and the board-order loop decides nothing; no effect both spawns a continuation and produces an event anything reacts to, so the order of those two queue pushes decides nothing either. Reverse the trigger loop, order it by uid, turn the queue into a stack, defer the death checkpoint to the end of the drain, read the acting order live, swap continuations with reactions: all six were free. `test/resolver-order.test.ts` now gates each one, **every test watched going red under its own mutation**, with the failures in `docs/learning/gate-proofs.md`. Three of the six needed a trigger that fires for more than one unit and the game has none, so `drain` and `resolvePhase` took an optional `extraTriggers` parameter that defaults to null and that production never passes — the one non-behavioural engine change here, made because a gate nobody can make go red is not a gate. The **within-unit tie-break was documented nowhere**: a unit carrying both Relay and Ward grants the power before the ward purely because Relay's `if` block is written first, and that sentence now lives in `triggersFor` with a test holding it. Two existing tests were passing for the wrong reason and are rewritten: the Guard-targeting test built its generator and threw it away, then indexed the target list with `i * 7919 % 2`, which alternates whatever the resolver does — it passed with `pick` stubbed to always return the first element; and "no unit acts after dying" took the unit off the board before the phase, so it exercised the phase loop's skip rather than the guard in `apply`, which could be deleted outright with the suite green. `cloneFight` **aliased both deck arrays**, so every lookahead rollout held a handle on the live fight's deck — harmless only until the first mill, shuffle-in or tutor effect, now copied and gated. Wake stays inert by the owner's decision, but a test now carries the whole sequence to the swing, so deleting the trait and fixing it **both go red** instead of a dead trait sitting behind a green assertion. Structurally, `ARCHITECTURE.md`'s one-way content → engine arrow was a cycle in the code: a fight is now handed a `CardPool` and the engine imports nothing, with `gate:boundaries` extended to name `src/content/` and its self-probe widened to prove it fires for **every** forbidden prefix rather than once in total. Behaviour held still and it was checked rather than asserted: `npm run verify` is byte-identical before and after, every win rate, gap and interval unchanged.
- 2026-09-06: **The gates the docs had been promising, made to go red** — three debts left by the two probes that landed this morning, all paid. `AGENTS.md` had a Gates section that was still a placeholder saying "no code, so no code gates" next to a repo with an engine, 34 tests and a simulation runner; it now lists only commands that exist and were run, and it says in as many words that **there is no lint gate and no format gate because there is no linter and no formatter** — a Gate written ahead of its command is the failure that section exists to prevent, and writing one now would have been the whole point missed. `exactOptionalPropertyTypes` went back to `true`, relaxed during integration because two sites assigned `undefined` explicitly against non-optional targets; both are fixed by conditional spread rather than a cast, because "this card has no bordure" means the key is absent, not present-and-undefined. Then the two gates `ARCHITECTURE.md` has been asking for since the day it was written: `npm run gate:boundaries` fails when anything under `src/engine/` imports from `src/render/`, `src/ui/` or `tools/` or references a `lib.dom`-only global, and `npm run gate:banned-apis` fails when `Math.random`, `Date.now` or `performance.now` appears outside `src/engine/rng.ts`. **Both were made to go red before being believed**, and the proofs are in `docs/learning/gate-proofs.md` with the mutations and the exact failures. The interesting finding is why they parse instead of grepping: on a completely clean tree, `grep` for those three names returns **16 hits and zero real calls**, because `rng.ts` and `measure.ts` both name all three in comments in order to say they are never called. A grep gate would have been red on arrival and then deleted or exempted into uselessness — a check built from the same symbol as the thing it checks proves only that the text agrees with itself. Both gates use the TypeScript compiler already in `devDependencies`, so no new package, and both carry the same two habits: a bound in the file header saying what a green run does not prove, and a probe they must make their own detector fire on before they will report an absence, so "did not run" cannot come back as "passed". The dependency-audit gate is `npm run audit` and sits deliberately **outside** `npm run gates`, because it reaches the registry and an offline machine would otherwise turn the per-commit gate red for a reason that has nothing to do with the code. One overlap recorded rather than churned: `test/determinism.test.ts` already had a line-regex version of the banned-API check, weaker on three axes, left alone because the prototype is under independent review at this revision.
- 2026-09-06: **70px heraldry works, and the thing that failed was not the thing we were watching for** — the first code in the repo: an SVG heraldry renderer driven by blazon strings in card data (`src/render/heraldry/`, eight charges, two tiers), sixteen fixture cards, and a Playwright probe that shot 158 views at two device scale factors. Every one of the sixteen was inspected individually at native resolution in both tiers on both grounds, per canon; the rows were used only for the adjacency question they actually answer. **The premise holds.** All four channels read at 70 device pixels, and Guard-on-the-silhouette is the strongest by a wide margin — a shield among rectangles is countable at a glance in a fifteen-card row. All six tribe pairs are separable at real size on both grounds. `ARCHITECTURE.md`'s instruction to check pairs visually rather than by palette is vindicated by its own numbers: dwarf-vs-human computes to **1.07:1 luminance contrast**, which would condemn one of the easiest discriminations on the board. What failed was a collision nobody was watching for — not two tribes against each other but **sable against the dark board**. The card outline `#0d0d12` is darker than both a `#26262b` field and a `#15171c` background, so a non-Guard dragon card has no edge at all: three of fifteen cards in the dark row simply are not there, and every adjacency trait depends on seeing which card is next to which. It is an outline defect, not a palette defect, and a pale mount stroked outside the dark outline fixes it on both grounds at no cost — verified against the same fifteen cards at the same size. Two smaller amendments: bordures should default to **or**, because an argent bordure merges with a light background, and the charge earns its place in the **compressed** tier rather than waiting for hover, since the eight stayed separable at 70px. The real floor is about **60 device pixels** and it is set by the numerals — silhouette and tincture survive to 44px and below, so if the board ever compresses further it is the numbers that need redesigning, not the heraldry. Four renderer defects were caught only by looking: a near-360° arc leaving a stray ring, a crescent that cannot be built as disc-minus-disc under either fill rule, a mount clipped away by the SVG viewport with no error, and two charges that read as a blob and a scarecrow. Bounds stated: no animation overlay, no human eye, eight charges rather than fifty, and no colour-vision-deficiency check — tribe is carried by hue and deuteranopia would likely collapse gules against vert.
- 2026-09-06: **Placement is a decision, and it is Ward that makes it one** — the first code in the repo: a headless deterministic prototype of one fight (`3209bbe`), built to answer the design's central falsifiable claim by observation. Over **20,000 seeded fights**, a bot that searches insertion positions wins **55.89%** against **40.02%** for a bot that places at random — a paired gap of **15.87 pp** (CI 15.13..16.61) against a measured noise floor of **0.04 pp** (CI -0.69..0.77), where the noise floor is two random-placement bots differing only in their placement stream. The methodology is the whole result, so it is verified rather than argued: card selection is a pure function of hand and energy that never reads the board, both arms receive the already-chosen card list and return only insertion indices, and deck shuffling runs on a **separate RNG stream** from targeting so that a placement changing how many targeting rolls a fight consumes cannot shift the draw order. The harness re-derives that from the runs — same hand, same cards, same order over **122,420 compared rounds** — and separately confirms the arms actually placed differently (51.6% of placed cards went to a different slot), because a same-cards check also passes when nothing differed. Three things the measurement was not looking for. **The gap is difficulty-dependent**, 17.78 pp on the hard encounter down to 1.08 pp on the trivial one, so any single figure is a figure about an operating point. **The negative control is the finding that matters**: strip Relay, Ward and Wake and the gap collapses to 0.40 pp at a 62% baseline — nowhere near a floor — so act order alone is worth about 1 pp and the adjacency traits are the entire effect, which is what the design says and now has a number. And **Ward, not Relay, carries most of it**: at matched baselines removing Ward more than halves the gap while removing Relay barely moves it. The design already contained the reason — its own worked example says Relay's flat +2 goes somewhere either way and only the packaging changes — so the decision is real but it is mostly about protection rather than buff routing. Then a design defect the prototype found by accident: ablating **Wake** changed the outcome of not one fight in 20,000, because a player unit can only die while the enemy is attacking and the enemy attacks after the player's whole line has resolved. Measured directly: **0** player-side deaths during the player's own phase against 1,111 during the enemy's, with 59 Wake grants all arriving after their unit had acted. Wake works only for the side that resolves second, which is structural rather than a numbers problem, and fixing it is a rule change the owner owns. Two smaller gaps recorded rather than patched: the design's worked example never says what keeps player attacks off the enemy hero, which its own targeting rule makes a legal target, so the example does not reproduce as written; and Ward's timing is unspecified, which matters more than it looks now that Ward turns out to be the load-bearing trait. Bounds stated with the claim: this measures bots, Bot B places uniformly at random and no human does, and against a naive fixed rule — always append beside the hero — the gap is 9.47 pp, which is the better guide to what placement is worth to someone not thinking about it. Nothing here says the game is fun.
- 2026-09-06: **Art is data: heraldic blazons rendered by code** — owner chose heraldic flat vector, Guard on the second visual channel, and unique art per card. Those three combine better than they read separately, because heraldry has a formal description language: a card's art is a **blazon string in its own data file**, rendered to SVG by code, and there are no image assets at all. Unique-per-card then costs combinatorics over a charge library of thirty to fifty rather than 250 bespoke drawings; 250 blazons are a few kilobytes against 250 PNGs that would strain canon's blob ceilings; artwork changes diff as text; and coherence is structural because one renderer draws everything. It also answers the compression constraint that drove the whole question — at fifteen units a card is about seventy pixels and no illustration survives that, whereas vector scales losslessly. The premise that seventy pixels gives two channels turned out to be true only of arbitrary illustration: heraldry separates identity into four independent layers, so **Guard takes the card outline and bordure** (shape reads faster than colour at small size and competes with nothing), tribe keeps the field tincture, numbers keep the corners, and the charge is the only thing needing the expanded tier. Rule that follows: no information required for a turn decision may live only on hover. Correction landed in both AGENTS.md and the design: "card art pipelines" was written as a non-goal before `pixel_lab` and `badge` were checked — the owner has a working sprite pipeline and a precedent for 300 coherent generated concepts, so the claim was wrong as reasoned; the absence of a raster pipeline is now stated as a consequence of the blazon system rather than a goal. Badge's contact-sheet lesson is carried in explicitly: 250 devices get reviewed individually at native resolution in both tiers, never as a grid, with tincture collision checked per pair at real size under an animation overlay.
- 2026-09-06: **Review was on the wrong node, and worktrees were conflated with file disjointness** — two owner questions, both landing. The agent graph said "three lanes because three disjoint directories", which implies disjoint files are enough for concurrency; they are not. Disjoint files buy clean merges, **worktrees buy safe concurrent operation**, and agents sharing one working tree collide on the index, `HEAD`, `node_modules`, build output and ports regardless of which sources they touch — the index worst, since `git add -A` sweeps in another agent's half-finished edits and ships a message that lies about its contents. The rule is now written against concurrency rather than files: none for serial node 0, one per phase-1 lane with its own install, one per concurrent content author. On review: canon scales it to risk and that is defensible — uniform review is unweighted rather than more rigorous, and a review that always passes proves as little as a gate that always passes. But the allocation was wrong. Weighting by **blast radius times gate weakness** puts the highest-value review in the graph on **node 0, the contract**, which had none: a type contract cannot be tested at all and getting it wrong invalidates three downstream lanes before anyone notices. The inverse is content, where a mis-costed card passes every test and human judgement of balance is unreliable — simulation is the instrument there and reading is not. Verified and reviewed are now stated as distinct claims, since a node can honestly be one without the other.
- 2026-09-06: **The agent graph, after misreading the question** — "graph engineering" was read as graphics and then as the resolver's effect DAG; the owner meant how *agents* are organised to build the game, and ARCHITECTURE.md now carries that as its own section. The load-bearing points: the fan-out gate is the **contract**, not a working engine, so node 0 ships types, fixtures and throwing stubs and all three lanes start against them rather than serialising behind the hardest node; **fixtures are what decouple the lanes**, since render and sim can otherwise write code but verify none of it, which is deferred work wearing parallel work's clothes; **edges are commits on main, not messages**, because agents cannot see each other's chats or trees, so a design requiring two agents to talk has a node missing; **fan-out width is set by directory disjointness**, not ambition, and widening means making things disjoint rather than adding agents to contested files; the **event log is the render contract**, which forecloses the predictable collision where the render agent adds `isCurrentlyActing` to `GameState` and corrupts the determinism hash; **cycles collapse into one agent**, so content and balance cannot be two workers each invalidating the other; content authoring may fan out but **balance never does**, being a whole-pool property; the **effect vocabulary is frozen** and needing a verb escalates, or four authors invent four synonyms and the resolver grows four code paths; and **review is a node with an edge**, sitting between worker and integration, because a review after merge is a report rather than a gate. Nothing built or verified — this is a plan, and the graph is a hypothesis about where the seams are that each wave tests.
- 2026-09-06: **Engineering plan, and Echo does not terminate** — `ARCHITECTURE.md` now carries the module graph, the resolution-queue contract, the visual and balance requirements, and the delegation plan. Writing the resolver contract surfaced a defect in the design of record shipped hours earlier: `Echo` read "repeat the action of the unit that resolved immediately before me", and two adjacent Echoes walk backwards forever — reachable deliberately once an Echo Sigil exists, so an engine iteration cap would have turned a buildable combo into a crash. Fixed in the rule rather than the engine: Echo copies the previous unit's **base** action, which terminates by construction and reads as one line on a card. Generalised in both documents — a trait that reads another trait's output needs an explicit termination argument before it is written. Three other things worth keeping: the module graph is one rule (`engine` depends on nothing, everything depends on `engine`) and it is enforced as an import-boundary lint rule rather than an intention, alongside a ban on `Math.random` outside `engine/rng.ts`, because determinism is what makes replay, balance simulation and bug reproduction all possible at once. The unlimited board must **compress and never wrap**, since a second row destroys the left-to-right reading every adjacency trait depends on. And the design's central claim is falsifiable as a number: run an optimal-placement bot against a random-placement bot, and if their win rates match, placement is not a decision — that gap is the game's reason to exist and it can go red in CI. Its bound is stated with it: it measures bots, not whether the game is fun. Nothing is prototyped or verified; this is a plan.
- 2026-09-06: **The repo joins the fleet and gets a design** — `cards` had been failing fleet's census gate as `NOT IN THE CENSUS`, so it would never have received the canon at all; adding it took `sync-canon` from 23 to **24 current** with `npm test` 84/84 (`fleet` `a7bdf58`, `cards` `d6422a0`). Its AGENTS.md was authored around a block rendered by fleet's own `canon.mjs`, so it classified `current` on the first check rather than after a repair. The design was then settled by five rounds of structured interview and written to `docs/design/game.md`: manual placement with automatic left-to-right resolution, random targeting with Guard as the only redirect, an equipped hero acting last, unlimited board width, units/spells/equipment, class-based characters over racial tribes, and sigils carrying both card modification and the run-wide effects that would otherwise have been a separate relic system. Two owner answers changed the shape more than their length suggests. **Random targeting** dissolved the largest risk in the first draft, where lane targeting and cascade order were two positional systems competing for one placement decision — placement now means cascade only. **Unlimited board width** was assessed as high-risk and that assessment was wrong: the owner pointed out units require cards, which require drafting, and the arithmetic agrees — energy, hand size and deck composition cap the board near six to nine units in play and about fifteen at the extreme, so the board self-limits at roughly twice Hearthstone's width and deck design is the real dial. What survived the correction is narrower and more useful: **token generation** is the only route to a genuinely unbounded line, because summoned bodies bypass all three gates, so the budget discipline belongs there and not on the board rule. Cascade traits are adjacency-only for the same reason — a counting trait's ceiling is whatever the deck economy currently allows, which is not a number anyone controls directly. Nothing is prototyped; every number in the design is a reasoned starting guess and the document's first line says so.
