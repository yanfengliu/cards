# 2026-09-23 — F1 gated by structure

Branch `worktree-agent-a85947414dcc7169f`, cut from `7d883bd`. `7d883bd` fixed finding F1 of the re-review of `2b040d9`: the fight check's expected enemy hero had been read off `encounterFor`, the function `fightSetupFor` builds the enemy from, so a change inside `encounterFor` moved both sides together. The fix restated the pick in `printedEncounter`. This round adds the gate that keeps it restated. The commits are `d3240fa`, the gate; `b621ad2`, two failure texts narrowed to what the code knows; `9ec0ed1`, two spellings the header claimed and the walk missed; `b88fe99`, what an independent review found; and `4554ecb`, one sentence added to the bound. No file under `src/` changed. The suite is 291 tests, from 289. Every mutation, its failure text and the runner are in `docs/learning/gate-proofs.md` under "F1 gated by structure".

## The fix could be undone without a sound

**Timestamp:** 2026-09-23 20:27–21:10 (`d3240fa`, `b621ad2`)

**Action:** `test/fight-check-reach.test.ts` reads `src/run/sigils.ts` and the files it reaches through the TypeScript checker. It works out two sets from the code on every run. The builders are every top-level function under `src/run/` that returns a `FightSetup`, and every function those reach outside `src/engine/`. The check is every top-level function and variable of `sigils.ts` in which a name has the type `Fight` or `FightSetup`, and everything those reach. The main test fails when the check reaches a builder with no setup builder between, or refers to a setup builder along more than one path. It also holds the three "never off", "nothing off" and "never through" sentences in `sigils.ts`: each name must be a builder, and each sentence must sit on a function in the check. A probe of every spelling the walk follows runs first, as its own test and again inside the main one.

**Result:** At every commit of this round the builders are `fightSetupFor`, `encounterFor`, `fightSeedFor`, `runPool`, `heroSpecFor`, `resolveDeckCard`, `heldHeroSigils`, `grantedTraits` and `sigilById`. The check starts from `seamSetup`, `fightSigilProblems`, `foughtSigilProblems` and `watchFights`, and one path leads to `fightSetupFor`. At `7d883bd`, `printedEncounter` rewritten to ask `encounterFor`, beside the review's M1, passed all seven gates, and so did the same rewrite through a helper. From `b621ad2` on, both stop `npm run gates` at `test`, and in each case the one test of 291 that fails is this one. `verify:run` still passes both, which it must: the rewrite changes no number.

**Reasoning:** The subject is a list of functions on each side, so the local rule on lists applies: the test reads both lists off the code and types neither. The builders are found by return type, not by the name `fightSetupFor`, so a second setup builder would be found too. The check is found by the types a declaration holds, which keeps `sigilProblems` out: it compares two run-layer records, holds no fight, and calls `sigilById`, which the fight is built from. A walk from every declaration of `sigils.ts` would have gone red on that call on a clean tree. The engine is left out of the builders because `printedEncounter` restates the pick with the engine's own `mixSeeds`, and that sharing is the fix, not the defect. The sentences are read rather than copied into the test, so a name added to one is checked the day it is written, and a stale name goes red. The walk follows any reference and not only calls, because a helper, an alias or a table of functions each reach a builder without a call to it in the check's own code. It is a test and not a `gate:` command: the chain refuses a `gate:*` script its list leaves out, and a new one would have meant editing the Gates section of `AGENTS.md`.

**Validation:** Rows G1 to G11 in `docs/learning/gate-proofs.md` are rewrites of the check, each red with the path from the check to the builder in its failure text. The P rows blind the walk one spelling at a time, and each is red in the probe on the spelling's own function. C1 and C2 are two things the header says it does not see, and both stay green. The base rows B1, B1a and B2 are the same pairs at `7d883bd`, passing.

## Two spellings the header claimed and the walk missed

**Timestamp:** 2026-09-23 21:18–21:29 (`9ec0ed1`)

**Action:** The walk resolves a shorthand destructuring - `const { k } = x`, at the top level or inside a function - against the type of the value it destructures. It resolves an index by the string-literal type of its key, rather than only a literal written in place. Three probe functions bind the new spellings.

**Result:** At `b621ad2` both of these passed the gate: `printedEncounter` doing `const { encounterFor } = nodes;` from a namespace import and calling it, and `printedEncounter` calling `nodes[key]` with `key` a `const` holding `'encounterFor'`. The header already claimed "a destructured export", and it implied a constant key was followed, since it named only "a key built at run time" as missed. From `9ec0ed1` on, both are red, rows G12 and G13. What the real check reaches did not change.

**Reasoning:** The first was believed covered because the module-level probe passed. That probe is written `{ heroOf: destructured }`, and the checker resolves a property name like `heroOf` by itself; the shorthand form has no property-name node, only the new local's own name. "A probe is evidence only for the spellings it contains", as the Gates section of `AGENTS.md` already says of the storage gate. The fix resolves only the shorthand form by hand, because doing the renamed form too would count one reference twice and throw off the path count.

**Validation:** P9 turns off the new shorthand branch and P10 narrows the index branch back to literals written in place. Each is red in the probe on its own function and nowhere else.

## An independent review walked past it twice

**Timestamp:** 2026-09-23 21:12–22:13 (`b88fe99`, `4554ecb`)

**Action:** A read-only review of `b621ad2` ran as a separate agent, in its own copy of the tree. It reported two edits that passed all seven gates, several sentences bigger than the code, and two false reds. `b88fe99` answers each. Whatever is handed as an argument to a setup builder, at any call in the files the test reads, is now a builder, and the check does not follow what it hands to its own setup builder, since that builds its subject. A named local function is a unit of its own, so two calls to it are two paths. A class's `extends` clause is followed. A function that only calls into the check is no longer added to it. The test name, rules 2 and 3, their failure texts and the bound were rewritten from the code. `4554ecb` adds one sentence to the bound.

**Result:** B1: the review handed `fightSetupFor` the encounter from its callers, which took `encounterFor` out of the builders. Rule 3 then failed, but its text said the fight was built from no function by that name and advised dropping the name. With the name dropped and `printedEncounter` asking `encounterFor`, and M1's effect summed off the ledger, all seven gates passed. At `4554ecb` the refactor alone passes, as it should (V1a), and the rest is red: `encounterFor is one of the builders: handed to a setup builder at src/run/run.ts:335` (V1b, and V1bg under the chain). B2: `seamSetup` built a second setup through a local closure called twice, and rule 2 counted one path. It is two now (V2a, V2ag). The false red on an aggregator of both halves in `sigils.ts` is gone (VF1). A second setup reading made on purpose still fails rule 2, by design, and its text now says so and where to widen the rule (VF2). The review's own edits are rows V1a to VC1, imported from its candidate file: 52 rows as wanted at `4554ecb`. `npm run verify` is byte-identical to `7d883bd`, `Elapsed:` included, and `npm run verify:run` differs only in `Elapsed:`.

**Reasoning:** B1 is F1 itself, coming back through a refactor of the builder rather than the check, so it had to be closed rather than bounded. What a setup builder is handed is what the fight is built from, whoever works it out. The check's own hand-off to its setup builder is the other side of the same line: it builds the subject, and following it would have failed the refactor the gate must allow. Dropping the rule that added callers to the check trades a thing no test showed (a caller that passes an expected value in) for a false red the review showed, and the bound already named arguments a caller passes in. Rule 2 counts places in the code, so it cannot see a setup built twice in a loop or a callback (V2b), and it can over-count a setup builder kept in a table or a class. That last came from a row that came back green: taking away the guard against counting a declaration's own name changed no probe result, so the guard is not what makes the count right. Both directions are in the bound.

**Validation:** P11 and P12 blind the argument rule each way, P13 walks local functions inline, and P14 skips `extends`; each is red in the probe on its own case. A first run at `b88fe99` had 48 of 52: three rows still expected texts `b88fe99` had reworded, and P15 came back green, which is where the over-count sentence came from.

**Code reviewer comments:** The read-only review above is the only independent review this round. It reviewed `b621ad2`, and it found B1 (material: F1 back with all seven gates green), B2 (material), sentences bigger than the code in the test name, rule 2, rule 3 and the assertion headline (material), a class reading the fight that the check does not read (minor, now in the bound), an `extends` clause skipped as a type (minor, fixed), the two namespace spellings already fixed at `9ec0ed1`, and two false reds. No review of `b88fe99` or `4554ecb` has run, and the multi-CLI review this repo asks for on high-risk changes has not run.

**Notes:** The previous round's F1r rewrite puts a `return` in front of the old body of `printedEncounter`. It passes the whole suite and `verify:run`, which is what that round ran, but `tsc` rejects the dead code behind the `return` (TS18048, `act` is possibly undefined). So under `npm run gates` it stops at `typecheck` and never reaches a test. A first run of this round's rows put that rewrite under the chain and got `UNCONFIRMED` twice. The chain rows now use a rewrite that typechecks, and the rows that reproduce F1r keep its own bytes where no typecheck runs. A later reword of the three sentences in `sigils.ts` can change what this test holds. It reads only a sentence that says "never" or "nothing", then "off" or "through", with "read" allowed between, then the names. A claim reworded out of that shape is no longer held, and the test fails only when none is left.
